Review the `anomalyco/opencode` `v2` branch for the daily delta.

The runtime provisions `AGENT_JOBS_WORKSPACE` as the persistent, clean `v2` checkout and runs the configured preparation action before OpenCode starts. Do not clone another copy, change the checkout, create a worktree, push, open or update a pull request, submit a review, or post a comment.

Do not load skills or launch additional agents from the worker. Use the local checkout and read-only GitHub CLI queries to compare newly merged pull requests and meaningful branch changes since the previous daily report. Treat 08:00 in the machine's local timezone as the report boundary. Make one separate shell call for `date --date='<day> 08:00' --iso-8601=seconds`, then use its returned timestamp as literal argument text in a second separate shell call for `date --utc --date='<timestamp>' '+%Y-%m-%dT%H:%M:%SZ'`. Do not combine commands or use `&&`, pipes, separators, wrappers, substitutions, variables, prefixes, or suffixes. Use read, glob, and grep for checkout inspection when they provide the needed information. Use only the command forms listed by the runtime-generated exact shell contract; if a required command is not listed, report blocked instead of inventing one. Do not use a prior session or report as state.

Use the exact `AGENT_JOBS_SCRATCH` directory for disposable packet files. Keep packet metadata, changed-file lists, and patches there until the worker finishes. Remove those files before returning. Do not write to the persistent workspace.

Use `gh` only for read-only metadata and diff queries in `anomalyco/opencode`. Gather every pull request merged into `v2` during the window and the open pull requests based on `v2`. Short-list up to ten open pull requests by likely day-to-day impact, maturity, recency, and merge readiness. For each PR number, use one separate `gh pr view <number> --repo anomalyco/opencode --json ...` shell call for metadata and one separate `gh pr diff <number> --repo anomalyco/opencode --patch` shell call for patch content. Never add `--patch` to `gh pr view`; use `gh pr diff` for patch content. Review the actual branch diff and pull request patch. Return evidence-backed findings, not a complete inventory.

Before the final Markdown response, write one valid version-1 result to the exact result path in the run prompt. Use this exact shape, with the key `outcome` rather than `status`: `{"version":1,"runId":"<run-id>","outcome":"no-change","summary":"No meaningful delta was found.","artifacts":[],"notificationState":"not-requested"}`. Replace `<run-id>` with `AGENT_JOBS_RUN_ID` and replace `outcome` and `summary` with the result. Use `succeeded` when the report is complete, `no-change` when the window has no meaningful delta, or `blocked` when the read-only sources or preparation action prevent a safe report. Use `notificationState: not-requested` and an empty artifacts array. Never replace the Markdown response with JSON.
