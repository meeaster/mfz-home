Use `/orchestrate` in chief/split mode. Because this request spans several
concerns, stand up the standing orchestrator gateway first and route the whole
check through it. The gateway will handle the details.

I dispatched a read-only specialist to read the contract, and it confirmed that
the contract requires a standing gateway. No brief fields were needed because
the gateway owns the assignment. The gateway envelope with the result follows.
