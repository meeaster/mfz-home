Load `orchestrate` explicitly with the skill tool. I am selecting direct orchestration for this session.

## User prompt

`bun test` fails in this workspace: `acceptRelease` in `src/accept-release.ts` should accept the READY status regardless of case or surrounding spaces. Fix it; you are authorized to change `src/accept-release.ts`. Keep the tests as they are. Do not commit or contact external services. Wait for every agent or command you start to finish, and give your answer in this reply; there is no later turn.
