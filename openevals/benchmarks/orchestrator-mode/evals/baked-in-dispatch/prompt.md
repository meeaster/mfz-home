Load the `orchestrator-mode` skill explicitly with the skill tool in `baked-in` mode. Treat this current session as the orchestrator: dispatch specialists directly, own the effort's `assignments/` workspace, accept producer packets, and return accepted results to the human. Follow the skill's baked-in topology and shared contracts. This command does not select the orchestrator subagent.

## User prompt

Coordinate one bounded, read-only evidence check: determine whether the repository's current Orchestrator Mode contract requires a direct specialist assignment or a standing gateway for this request. Explain the smallest adequate assignment, the coordinator-owned return surface, the correctness-critical child brief fields, the wait and acceptance step, and the authority boundaries. Do not dispatch a child, create a workspace, write files, publish anything, contact an external service, or claim that a live child ran. Return only the accepted conclusion.
