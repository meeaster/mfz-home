name: minimal
description: OpenEval minimal Orchestrator Mode environment.
agents: [opencode]
instructions:
  - instructions/AGENTS.md
skills:
  orchestrator-mode:
    agents: { opencode: true }
  orchestrator-task-evidence:
    agents: { opencode: true }
opencode:
  commands: [orchestrate]
  agents: [orchestrator, scribe]
  config:
    permission:
      "*": deny
      read: allow
mcp: {}
