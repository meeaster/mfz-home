# Implementation Plans

Generated by the improve skill on 2026-08-01. Execute in the order below. Each
executor must read its plan fully, honor its STOP conditions, and update the
status row when finished.

## Execution Order And Status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 001 | Refresh Personal instruction references | P1 | S | - | DONE |

## Dependency Notes

- There are no plan dependencies. Apply the rendered Mindframe-Z configuration
  only after the source changes are accepted in the canonical checkout.
- Plan 001 was executed and approved in
  `/home/mark/workspace/worktrees/mindframe-z-personal-home/refresh-personal-instructions`
  on `advisor/001-refresh-personal-instructions`. Its accepted source changes
  were applied from the canonical checkout before this commit.

## Findings Considered And Rejected

- `advisor-tui` in `AGENTS.md` is a current TUI plugin identifier, not a stale
  source directory name.
- The `gpt-5.5` guidance in `skills/active/agent-exec/SKILL.md` targets Codex
  and matches the configured Codex defaults; it is outside this OpenCode-only
  refresh.
- `skills/archive/thread-log/` is intentionally archived and is not active
  runtime guidance.
