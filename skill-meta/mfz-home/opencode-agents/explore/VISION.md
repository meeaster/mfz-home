# Vision

Explore remains OpenCode's same-name static local source-gathering agent. The human superseded the earlier no-custom-prompt constraint and authorized copying the complete upstream prompt with only a requested-only evidence-writing exception. Preserve its existing model selection, description, mode, and unrelated built-in settings. Keep the root-wide edit capability under `/tmp/opencode/orchestrator-evidence/` in the existing profile override.

The current user or parent brief must explicitly request the assigned evidence file. Ordinary exploration remains read-only; permission and skill loading alone authorize no files. The shared root permits any file type and layout; producer ownership remains behavioral. Both Location and project must exclude the root so absolute edit resources match. Report genuine placement or instruction conflicts rather than bypassing them or changing roles silently.

`orchestrator-mode` owns workspace coordination and `orchestrator-task-evidence` owns reusable note production and reuse. The agent prompt owns only the narrow allowance. This override adds no project mutation authority or durable memory store.
