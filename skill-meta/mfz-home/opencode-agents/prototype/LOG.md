# Log

## 2026-08-18 - Permanent prototype agent

- Promoted one model-independent `prototype` subagent after fifteen matched runs across logic, UI, and unsettled-judgment scenarios.
- Selected Terra `max` for the base profile because it produced the strongest Terra evidence at an aggregate estimated cost of `$0.9280776`.
- Selected Sol `medium` for the Personal profile because it produced the strongest decision artifacts, especially on the unsettled-judgment scenario.
- Kept the behavioral contract in the existing `prototype` skill and denied recursive delegation.
- Preserved the model-specific candidates and their records as evaluation evidence.

## 2026-08-18 - Evaluation candidates removed

- Removed `prototype-luna`, `prototype-terra`, `prototype-terra-xhigh`, `prototype-terra-max`, and `prototype-sol` after the experiment report captured their results.
- Kept the scratch artifacts and `docs/prototype-agent-evaluation.md` as the experiment evidence.
