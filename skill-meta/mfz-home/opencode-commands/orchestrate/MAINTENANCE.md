# Maintenance

## Runtime dependencies

- OpenCode must expose native `explore`, `research`, `inspect`, `triage`, `architect`, `agent-author`, `prototype`, `worker`, `reviewer`, and `pr-reviewer` subagents.
- The primary session owns coordinator model and reasoning-effort selection; the command must not override either.
- `context-transfer` owns the parent-to-agent brief contract.
- The root-owned `session-analyst` supplies targeted reconstruction from durable OpenCode session history when active context has a material continuity gap.
- The `prototype` skill owns the native prototype agent's logic, state-model, and UI artifact procedures.
- The `pr-review` skill owns holistic merge due diligence; the native `pr-reviewer` owns its read-only execution boundary.
- Each agent's definition and authoring record own its model, permissions, and role boundary.

## Change procedure

Preserve the argument-placement invariant: `$ARGUMENTS` appears exactly once, unwrapped, as the final content under `## User prompt`. The opening identifies that section as the user's current input and permits natural interpretation of conversational forms without prematurely classifying them as a settled task, implementation request, authority grant, or workflow phase.

1. Read this record and the records for every affected agent before changing routing, acceptance, or authority behavior.
2. Keep the command generic. Put project, OpenSpec, review-method, and domain-specific procedure in the owning workflow or child brief.
3. Preserve explicit user authority for every `architect`, `agent-author`, `prototype`, `worker`, `reviewer`, and `pr-reviewer` dispatch. Read-only `explore`, `research`, `inspect`, and `triage` fan-out remains coordinator-owned and grants no later-phase authority.
4. Preserve the narrow planning exception: the primary may record an explicitly requested accepted design through its owning workflow, but that authority does not include implementation.
5. Preserve asymmetric context transfer: rich decision-relevant context goes to fresh children, while compact evidence packets and bounded reusable memory return to the primary session.
6. Keep judgment with the coordinator and the human decision surface in the main session. Children gather bounded evidence or offer consultation; the coordinator challenges, weighs, recommends, and synthesizes, while the human explicitly accepts or revises architecture before implementation is proposed or its authority requested.
7. Select source gatherers by evidence source and required operation: static local file evidence uses `explore`, authoritative external documentation and upstream-source facts use `research`, and current or command-derived repository, runtime, cloud, deployment, or work-system facts use `inspect`. Read-only evidence gathering that requires command execution never goes to `explore` or `research`; split materially different evidence classes into bounded units. Keep `triage` goal-oriented around one reported symptom rather than using it for generic shell work.
8. Keep mutation authority outcome-scoped; do not silently expand commit into push, push into pull request or merge, draft into publication, or one system change into another.
9. Keep `explore`, `research`, and `inspect` fresh by default for every bounded unit. Resume only for direct continuation of the same unresolved investigation and downstream decision, in the same role and materially overlapping evidence family, with no independence need and retained value that compact message memory cannot preserve. Topic or repository overlap is insufficient.
10. Keep routing threshold-free and telemetry-optional. Known context burden, staleness, compaction, noisy traces, failures, retries, and unrelated work favor freshness; absent telemetry is neutral. Provider cache reuse is secondary and never independently justifies resume. Do not use `session-analyst` or trace archaeology solely for routine routing estimates.
11. Keep reusable memory message-based, latest-only, and separate from the decision packet. Preserve its source session ID and five fields: decision context, verified evidence with locators and freshness, open work, warnings, and routing guidance. Each successor rewrites a compact replacement; no filesystem artifact or recursive memory chain is introduced.
12. Keep code-heavy architecture synthesis with `architect` while user dialogue and final decisions remain with the coordinator. Before every dispatch or resume, explain its value at the current point, summarize gathered evidence and the pending decision, and obtain explicit human approval; initial approval grants no later redispatch. Explain configured specialist expense without hardcoding models, providers, prices, or durable cost tiers. Afterward, surface the substantive packet with its credible options, evidence and inferences, strongest cases and tradeoffs, recommendation, uncertainty and gaps, and reversal conditions; require explicit human acceptance or revision before proposing implementation or asking for its separate authority. Require credible options where they exist, not an artificial quota.
13. Keep all architecture-specific `explore`, `research`, and `inspect` sessions root-owned. Assess every single or batched architect request for materiality, duplication, accepted scope, read authority, and existing-child overlap; run independent authorized units concurrently and dependent units serially.
14. Preserve the transport boundary: detailed evidence stays in gatherer sessions; do not replay transcripts, add a custom session-result tool, or teach the architect to retrieve sessions through OpenCode CLI or API.
15. Preserve recursive architect delegation denial. Reconsider it only when repeated live evidence shows material root-mediation delay or primary-context bloat that outweighs shared evidence ownership and simpler topology.
16. Preserve phase-aware guidance: recommend one next step, classify OpenSpec proportionally, and make risk-based review recommendations only after implementation. Agent-author completion receives coordinator verification without a review recommendation; a reviewer remains available only by independent user request. Recommendations never grant authority.
17. Route explicitly authorized AI-consumed instruction changes to `agent-author`, keep general application mechanics with `worker`, and preserve their interface when mixed work is split.
18. Keep agent authoring outcome-scoped; it does not imply publication, source-control operations, unrelated application changes, or independent approval. Inspect its artifacts, run focused validation, report uncertainty, and complete coordinator acceptance without recommending review.
19. Preserve proportional build preflight. Once authority, outcome, and scope are settled, decompose implementation before scheduling, then dispatch each ready mutation owner directly. Keep ordinary immediate checks with that owner even when read-only, and use `inspect` only when read-only evidence is the outcome, mutation lacks authority, or readiness crosses a distinct live-system, access, or human-decision boundary. A separate authorized worker may own substantial mechanically separable preparation that changes the brief or state or protects build context.
20. Size implementation units semantically. Require one primary accepted outcome, one coherent ownership boundary, and compatible acceptance and validation, informed by owned mutable state, implementation domain, external state, dependencies, and integration needs. Keep tightly coupled changes together and split materially independent plugin or subsystem domains, lifecycle concerns, authority boundaries, validation environments, or external systems; never split mechanically by file, directory, repository, or count.
21. Separate scheduling from scoping. Serialize mutation by default and whenever overlap is unknown. Treat checkout and Git state, lockfiles, installation or dependency state, generated output, test caches, active runtimes, schemas, and external systems as shared unless explicitly isolated. Parallel mutation requires demonstrable independence, material value, and any separately authorized isolation.
22. Keep integration mutation with an authorized mutation owner. Isolated parallel workers return evidence and defer shared state. Dispatch a separately authorized integration worker only when combining results requires mutation or a distinct shared validation outcome; otherwise leave coherent final validation with the last owning worker and avoid a ceremonial integration unit. The coordinator never integrates by mutation.
23. After substantial preparation, require parent verification and normally start a fresh prototype or implementation agent with a narrow accepted-state brief. Sequential workers consume verified current state and compact relevant handoffs. Do not create worktrees automatically; follow dirty-state and preparation authority rules.
24. Preserve dirty state before Git topology or synchronization mutations, resolve "get latest" into a specific operation, and keep Git, Jira, and environment authority separate unless one requested transaction inherently combines them. Observing status alone is inherent preflight; a real dirty-state handling choice returns to the human.
25. Keep the preparation handoff fields and narrowed build brief aligned with `EVALS.md`; they are the observable boundary that prevents setup context from leaking into the artifact build. Avoid duplicate source reading, validation planning, and state checks between preparation and mutation children.
26. Keep durable OpenCode session history as the sole continuity and trace-evidence backbone. The active summary and context are the default continuation path after compaction.
27. Preserve proportional recovery. Assess whether spanning work has a material continuity gap before dispatching `session-analyst`; new post-compaction work and same-work continuation with a sufficient summary require no reconstruction.
28. Keep analyst requests phase-targeted and question-led. Cross the relevant compaction boundary, follow a multi-compaction phase from its meaningful start when needed, and reject routine or whole-session reconstruction.
29. Require the compact packet fields in `EVALS.md`, then treat the packet as evidence: check active context, resolve conflicts or bounded gaps through focused follow-up, and synthesize only what changes continuation.
30. Keep persistent artifacts behind separately requested owning workflows. Session reconstruction itself does not create a handoff, Session Brief, work-context checkpoint, OpenSpec artifact, or parallel continuity record.
31. Check all relevant packets, surface architect-requested evidence in the main session, then obtain explicit human direction before returning it. When approved, resume the same architect once with compact packets, session IDs, results, gaps, and material delta rather than forcing one resume per gatherer.
32. Keep `prototype` limited to explicitly authorized runnable artifacts for one bounded unsettled logic, state-model, or UI question. Discussion and generic technical-feasibility spikes remain outside the mutation lane.
33. Keep prototype procedure in the owning skill, interpretation and decisions with the coordinator, and production implementation behind separate worker authority. Prototype creation never implies commit, push, pull request, publication, Jira updates, deployment, or productionization.
34. Route reviews by evidence contract, not authorship provenance: known accepted design, implementation brief, and validation history use focused `reviewer`; unknown or unobserved rationale, implementation, or validation requiring reconstruction and challenge uses `pr-reviewer` through `pr-review`.
35. Keep broad PR gathering root-owned. Reuse existing evidence and retained sessions, select only materially needed source-gatherer roles, keep traces in child sessions, and brief the reviewer with compact packets, session IDs, confidence, gaps, constraints, charter, authority, and stop conditions.
36. Preserve the complete missing-evidence request cycle: assess every unit, reuse relevant evidence, apply source-gatherer continuity rules, parallelize independent units, serialize dependencies, check packets, and normally resume the same reviewer once with compact results and delta. Packet conclusions remain evidence rather than acceptance.
37. Keep reviewer tool use focused and keep source/current/external evidence requests distinct from architect consultation, user decisions, mutation, expanded access, and publication authority. Retain merge and design decisions in the coordinator and do not run architect routinely before PR review.
38. Keep both review lanes explicitly authorized and parent-adjudicated.
39. Keep non-source continuity role-specific. Resume an architect within one downstream design engagement, including correction and reconsideration, but start fresh for materially new boundaries or a separately approved independent opinion. Resume triage within one incident. Start authoring, prototype, and implementation agents fresh per genuinely distinct accepted unit, but resume a cleanly stopped child when one bounded user decision continues the same underlying objective and artifact or work unit, relevant context and safely preserved state still carry material value, and authority is sufficient. Mechanism, provenance, route, placement, or blocker changes do not alone define a new unit; accepted outcome, primary ownership, artifact set, authority, system boundary, validation contract, contaminated context, valueless preparation history, or independence may. Start review roles fresh for initial independent judgment and resume only for bounded evidence completion or conflict adjudication in the same unconcluded review. Never send repair to a reviewer or make post-remediation review mandatory.
40. Treat these non-source defaults as provisional. Revise them only from observed orchestration traces during an explicitly requested post-hoc assessment. Routine dispatch uses no automatic session analysis, numeric threshold, usage estimator, or generic cheap-versus-expensive classification.
41. Preserve the worker-stop interface in caller briefs. This command supplies each implementation worker's bounded troubleshooting, qualitative context-pressure, stop, blocker-packet, no-self-dispatch, and design-evidence contract, and each blocker triage session's read-only diagnosis, packet, contract-fit, and design-flag contract. Compaction alone never forces a stop; a broad plan surviving compaction, repeated post-compaction discovery, unrelated active streams, or approach to another compaction without a validated intermediate outcome favors handoff. Keep all token, context, turn, tool, compaction, retry, and troubleshooting thresholds non-numeric. The promptless agent definitions own only their stable role descriptions and permissions. This command also owns packet checks, classification, fresh-versus-resume routing, evidence gathering, human visibility, architect gating, and dispatch authority.
42. Start triage fresh after a looping worker and omit raw failed-command history. Prefer a fresh remediation worker with checked diagnosis and current state. Keep this separate from a clean stop on one bounded decision, where the general same-work-unit continuity test applies.
43. Keep possible design conflict as evidence. Gather the smallest root-owned source evidence, return the decision to the human, and use only the existing approved architect under the every-turn gate. Requirement, scope, access, and authority changes also return to the human.
44. Keep stronger-worker machinery deferred. Add no role, profile or model change, command override, or automatic escalation unless later comparable traces meet the evidence threshold recorded in `VISION.md` and the human authorizes a separate revision.
45. Preserve frontmatter-only `worker` and `triage` files so OpenCode inherits the provider system prompt. A custom body is a separate redesign, not part of this orchestration protocol.
46. For explicitly authorized coordinated work across known repositories, allow one worker to proceed sequentially while preserving repository-specific instructions, validation, Git histories, commit boundaries, remotes, and authority. For Git publication, treat publication as non-atomic, prefer validating every repository before the first publish when practical, create separate Conventional Commits only when authorized, require per-repository results, and preserve partial publication without destructive rollback. Repository count alone does not split this operational outcome; split only for genuine independence and material parallel value.
47. After mutation, perform focused coordinator acceptance verification for every affected repository or system. For Git publication, check status, commit identity, branch and upstream relation, and push state as applicable without commissioning another broad preflight.
48. Update affected scenarios in `EVALS.md` and record consequential decisions or reversals in `LOG.md`.

## Validation

1. Run plain `mfz apply` from the Personal home when the current source state is ready to render.
2. Confirm `$ARGUMENTS` occurs exactly once as the unwrapped final content under `## User prompt`, and confirm the opening does not call the input "the task."
3. Confirm the rendered command has no model override, has `subtask: false`, and remains one runtime Markdown file.
4. Compare the rendered command byte-for-byte with its source and run `mfz doctor`.
5. Inspect resolved architect permissions and confirm recursive delegation remains denied.
6. Review the conversational-input scenarios and the representative authority, semantic decomposition, sequential-default and unknown-overlap scheduling, isolated parallel exception, integration ownership, compaction counterexample and broad-plan handoff, direct mutation-owner preflight, material preparation, coordinated multi-repository partial publication, coordinator acceptance, worker progress and stop packet, fresh blocker triage, checked-diagnosis transfer, bounded continuation across mechanism or placement correction, generated-skill authoring example, possible design conflict, architect correction and anchoring, architect evidence return, triage continuity, distinct-unit freshness, review evidence completion, optional fresh rereview, fresh source-gatherer continuity, reusable-memory, trace-grounded maintenance, and proportional reconstruction scenarios in `EVALS.md`. Static evaluation is sufficient for an instruction-only change; do not run a live mutation or architect consultation solely as validation.

## Environment boundary

This command is specific to OpenCode and this Mindframe-Z home. It inherits the current primary session's model and reasoning effort.
