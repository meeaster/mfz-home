# Maintenance

Preserve the thin explicit skill-tool loader, human-facing context, current-session execution, and single final argument boundary. OpenCode V2 does not expand `@path` in stored commands; do not replace the explicit skill load with a file attachment assumption.

Workflow edits and behavioral scenarios belong to `../../skills/orchestrator-mode/`. Keep that runtime in one SKILL.md and update it rather than copying instructions into this command or the orchestrator agent. Verify MFZ dry-run output and compare the command materialization to source before an authorized production apply. Do not apply, commit, or publish merely to validate authoring.
