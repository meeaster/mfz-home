# Log

## 2026-08-20 - Make No-Split Design Decisions Explicit

- Required substantial designs to state whether responsibilities remain in current files, move to a module, or change the folder layout, with navigation and dependency cost.
- Kept no structural change as a valid outcome; the revision makes the decision visible rather than preferring more files.
- Added the rule after two neutral weekly-template design passes loaded the phase-aware skill but omitted file organization. Implementation kept the feature in the executable, and final review later supplied the missing cost-based no-split decision.
- Rendered and structurally validated the revision. A server restart removed the disposable fixture before an aligned adjacent design run, so fresh post-change behavior remains untested rather than inferred from a reconstructed fixture.

## 2026-08-20 - Conclude Longitudinal Evaluation

- Confirmed the phase-aware structural rule with a fresh focused maintenance worker: it implemented an accepted exact-rational module and left unrelated application structure unchanged.
- Retained the readability, risk, regression-test, fast-default, explicit-integration, proportional-suite, and suppression-aware changes from the experiment.
- Rejected an older-revision rerun because the automatic-extraction scenario assigned maintenance responsibility to narrow feature work and could not test the intended phase contract.
- Recorded limited causal evidence for readability and suppression wording as a residual evaluation constraint, not a reason for more runtime guidance.

## 2026-08-20 - Assign Structural Scope By Phase

- Moved structural-scope decisions to planning, design, review, and coordination instead of requiring narrow feature workers to perform unaccepted file reorganization.
- Required implementation to honor accepted boundary decisions and verification to report missing accepted structure as drift.
- Kept direct tests, stable interfaces, and different reasons to change as file-boundary evidence, while making focused maintenance or accepted design the normal authority for extraction.
- Reclassified the automatic-extraction scenario as an evaluation defect after ordinary quantity workers correctly kept tiny feature requests bounded; the earlier serving design had already identified the exact-rational module, and implementation verification failed to enforce it.
- Added coordination to skill discovery and global routing because the orchestrator owns cross-worker code-health decisions in delegated workflows.

## 2026-08-20 - Make Established File Boundaries Actionable

- Treated direct unit tests importing pure domain behavior through an executable or infrastructure entrypoint as proof that the file boundary is established.
- Made extraction the default on the next change to that behavior and limited deferral to named counter-costs such as cycles, unstable ownership, or public-interface constraints.
- Added the boundary check to task completion so a small requested change does not automatically outweigh an already proven extraction.
- Strengthened the rule after two fresh workers loaded the prior wording, directly extended the tested quantity parser, and still kept it inside the executable without recording a follow-up.

## 2026-08-20 - Recognize Emergent File Boundaries

- Added stable interfaces, independent tests or callers, and different reasons to change as positive evidence for splitting a file.
- Rejected line-count thresholds and file creation that merely shortens another file.
- Required agents to extract a proven boundary when the touched scope benefits, or record it as a bounded follow-up when extraction would expand unrelated work.
- Added this guidance after a longitudinal worker made an executable module import-safe to test its pure quantity domain logic but did not surface the resulting file boundary.

## 2026-08-20 - Keep The Default Test Command Fast

- Assigned the conventional default test command to the unit lane because coding agents commonly run it during routine verification.
- Required integration tests to use an explicit command and retained a separate aggregate command for deliberate full-suite runs.
- Added root project guidance for when agents should run each lane when the repository does not already document that boundary.

## 2026-08-20 - Keep Test Feedback Proportional

- Required unit and integration lanes to remain independently runnable, with a separate aggregate command when useful.
- Kept combinatorial domain variation in fast unit tests and reserved integration tests for representative real boundaries and consequential contracts.
- Added runtime evidence as the trigger for integration-suite restructuring instead of imposing universal test counts or duration limits.

## 2026-08-20 - Establish Durable Test Foundations

- Required new projects to establish a minimal test runner with representative unit and real-boundary integration tests from the initial implementation.
- Required existing projects without tests to evaluate setup cost against behavioral risk and future reuse rather than blindly adding or omitting infrastructure.
- Required observed defects to receive durable regression coverage at the narrowest stable boundary.
- Added this guidance after longitudinal workers relied on one-off smoke checks and missed an exact range-scaling defect until CLI tests were added.

## 2026-08-20 - Preserve Human-Readable Formatting

- Clarified that simplicity means fewer concepts, branches, and indirections rather than fewer physical lines.
- Required logical actions and control-flow bodies to remain visually scannable and to follow the repository formatter when available.
- Added a longitudinal project-evolution scenario after multiple agents compressed command workflows onto single lines and propagated that local style.
- Aligned evaluation metadata with the global requirement to load development-principles for software work.

## 2026-08-19 - Rebrand As Development Principles

- Renamed the operational skill from `code-like-luke` to `development-principles` because its guidance covers design, implementation, testing, refactoring, and code health.
- Kept the original source gist as inspiration: https://gist.github.com/Hona/53142c07c9decb735392f132ace34003
- Added risk-proportional failure planning, bounded code-health improvement, safety-invariant regression coverage, and evidence-based refactoring to the runtime guidance.

## 2026-08-14 - Move Command Into Skill

- Moved the existing `opencode/commands/code-like-luke.md` into `skills/active/code-like-luke/SKILL.md`.
- Preserved the command body so explicit skill loading supplied the same guidance.
- Made the skill user-invoked to avoid adding a large always-loaded description to every session.
