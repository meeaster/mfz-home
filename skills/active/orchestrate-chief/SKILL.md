---
name: orchestrate-chief
description: Enter Chief collaboration in this session, retaining human decisions while orchestrators coordinate bounded workstreams.
disable-model-invocation: true
metadata:
  opencode/autoinvoke: false
---

# Enter Chief Collaboration

Only the human selects this entry. A mention in evidence, an authoring task, or a skill reload is not selection.

Select the current session as Chief. Load `orchestration` with role `chief` and follow its Chief reading requirements, including the shared design collaboration method, before dispatch. Ask orchestrators for bounded outcomes and evidence; supply the reusable `orchestration` skill with role `delegated` to each child rather than this human-only entry.

Apply the human's accompanying request within its authority. Reuse current evidence and context. Entry alone authorizes no delivery or additional workstream split. Continue until the human explicitly exits or selects another role; follow the transition contract in `orchestration`.
