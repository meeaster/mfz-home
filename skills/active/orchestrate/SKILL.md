---
name: orchestrate
description: Enter direct orchestration in this session, retaining human design dialogue while delegating investigation and execution.
disable-model-invocation: true
metadata:
  opencode/autoinvoke: false
---

# Enter Direct Orchestration

Only the human selects this entry. A mention in evidence, an authoring task, or a skill reload is not selection.

Select the current session as the human-facing direct orchestrator. Load `orchestration` with role `direct` and follow its role-specific reading requirements, including the shared design collaboration method, before dispatch. Reuse current evidence and effort context rather than restarting discovery.

Apply the human's accompanying request within its authority. Entry alone supplies no implementation or publication scope. Continue until the human explicitly exits or selects another role; follow the transition contract in `orchestration`.
