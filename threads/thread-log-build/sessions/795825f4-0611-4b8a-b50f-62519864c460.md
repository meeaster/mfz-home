---
title: "Add a sources bucket to thread-log; evaluate first-run subagent extraction; set subagent depth as a consumer choice"
thread_relevance: "Core: shapes thread-log's extraction schema (sources as a first-class state bucket) and establishes the principle that subagent-read depth is the consumer's policy, not the session skill's — the seed of read_subagents."
gaps: "Subagent analysis here is the *parent's* reading of an earlier ps-watchtower run's four subagents; those are reported as the parent observed them. Turn numbers approximate."
extracted_by: "opus-4.8 high"
---

# Session 795825f4 — Sources bucket, first-run subagent evaluation, skill-independence principle

## Decisions

- [2026-06-24 19:09] Add `sources` as a new first-class **state** bucket (peer of `artifacts_touched`), capturing *external* references only — docs, URLs, library docs, reference repos, Jira/Confluence. Two forks settled: (a) external-only vs mixed internal/external → external-only; (b) scavenge sources from existing buckets vs a dedicated bucket → dedicated bucket. Reason: a single source of truth, killing the digest-time scavenge logic. (795825f4 · turn 4)
- [2026-06-24 19:41] In `claude-code-sessions`, mandate `jq` and forbid `Read` on full JSONL transcripts — "a session transcript routinely exceeds Read's size cap." First-run agents wasted turns hitting the cap, so make jq the rule, not the recovery path. (795825f4 · turn 30)
- [2026-06-24 19:47] Subagent-read *depth* is the **consumer's** choice (thread-log's extraction contract), not baked into `claude-code-sessions`. Fork: universal rule in the session skill vs consumer-decides → consumer decides. Reason: "claude-code-sessions is an inpendent skill and is meant to be used for many different scenarios." (795825f4 · turn 36)
- [2026-06-24 19:47] Capture a subagent's input + output from the **parent** transcript's Agent tool_use / tool_result pair — never open the nested `agent-*.jsonl`. Reason: the parent already holds both, so this is materially cheaper and token-considerate. (795825f4 · turn 36)
- [2026-06-24 19:36] Drop reasoning/thinking parts from extraction — "i am not sure we care about reasoning parts." (795825f4 · turn 22)
- [2026-06-24 19:53] Create a matching spec doc for `claude-code-sessions` (mirroring `opencode-sessions`), for structural parity across the parallel session skills. (795825f4 · turn 48)

## Learnings

- [2026-06-24 19:30] First real extraction run (ps-watchtower) had two failure modes: Claude Code agents wasted turns hitting Read's token cap before falling back to jq; OpenCode agents under-read by stopping at the session boundary and not following nested subagents / cross-session references. (795825f4 · turn 20)
- [2026-06-24 19:47] A subagent's prompt lives in the parent's `Agent` tool_use.input and its result in the matching tool_result (here ~14KB of already-distilled text) — so "capture subagent I/O" means reading the parent, never the nested file. (795825f4 · turn 36)
- [2026-06-24 19:09] Before this change, the digest's Sources section was *scavenged* from `learnings` + `artifacts_touched` at digest time — error-prone, no single home. (795825f4 · turn 4)

## Mistakes Fixed

- [2026-06-24 19:45] The assistant first drafted prescriptive depth language *into* `claude-code-sessions` ("shallow read is your default"). The user corrected it — the skill is independent; depth belongs to the caller. Reverted to neutral capability language and moved the policy into thread-log's contract. (795825f4 · turn 36)

## Intent & Vision

- [2026-06-24 19:05] "Can we update the thread-log skill to include sources in session markdown files." (795825f4 · turn 1)
- [2026-06-24 19:15] "there was another session we used to build this skill. it was accidently started in ~/work/ps-watchtower... it started up some subagents to explore and i am wanting to evaluate how each of those explored and see if wee need to update the session skills or this skill guidance so we aren't missing valuable information" — the dogfooding instinct: study a real run to find where extraction loses information. (795825f4 · turn 8)
- [2026-06-24 19:36] "we do have to remember that we wnat to be considerate of how much tokens we are reading from this session so that way we don't spend too much. We should just always use jq operations. i am not sure we care about reasoning parts." — cost-consciousness as a first principle of the design. (795825f4 · turn 22)
- [2026-06-24 19:45] "Remember that claude-code-sessions is an inpendent skill and is meant to be used for many different scenarios. The consumer of the skill should define how indepth we want session exploration" — the separation of concerns that keeps the session skills reusable. (795825f4 · turn 36)

## Artifacts Touched

- `skills/thread-log/SKILL.md` — added `sources` bucket; removed hardcoded "six buckets" count; reclassified sources/artifacts as state.
- `skills/thread-log/INGEST.md` — bucket-count-agnostic wording; digest Sources now collated from `sources` buckets; subagent I/O captured from parent records, not nested files.
- `docs/skill-specs/thread-log.md` — synced sources bucket into the spec.
- `skills/claude-code-sessions/SKILL.md` — hard rule: always jq, never Read on full JSONL.
- `skills/claude-code-sessions/SESSIONS.md` — removed depth prescriptions; left capability neutral.
- `docs/skill-specs/claude-code-sessions.md` — created, mirroring opencode-sessions' spec structure.

## Sources

- `writing-great-skills` skill — invoked to frame the edits.
- `claude-code-sessions` / `opencode-sessions` skills — read and edited.
- Prior ps-watchtower thread-log run (session `b4945a55`, four Explore subagents) — the empirical evidence for the jq and depth findings.
- `docs/skill-specs/opencode-sessions.md` — the template for the new claude-code-sessions spec.
